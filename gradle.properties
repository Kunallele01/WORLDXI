# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# This machine tends to be low on free RAM (lots of other apps/IDEs running), and the
# Gradle daemon crashed once with an out-of-memory native allocation failure. Keeping
# the daemon heap modest and limiting parallel workers makes the build fit in whatever
# RAM is actually free instead of fighting other processes for it.
org.gradle.workers.max=2
kotlin.daemon.jvm.options=-Xmx1024m
# When enabled, the Configuration Cache allows Gradle to skip the configuration
# phase entirely if nothing that affects the build configuration (such as build scripts)
# has changed. Additionally, Gradle applies performance optimizations to task execution.
org.gradle.configuration-cache=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# AGP 9's built-in Kotlin compilation is stricter about how Kotlin source sets get
# registered; KSP (used by Room/Hilt) still uses the older kotlin.sourceSets DSL to
# wire up its generated-code directory, which trips AGP's new check. This is the
# workaround AGP itself suggests until KSP updates to the new API.
android.disallowKotlinSourceSets=false
# AGP 9.2.1 was only tested up through compileSdk 37.0; we're on the newer 37.1 platform
# release, which triggers a "not yet tested" warning on every build. It's just a warning
# (the build works fine) — this silences it until AGP catches up.
android.suppressUnsupportedCompileSdk=37.1