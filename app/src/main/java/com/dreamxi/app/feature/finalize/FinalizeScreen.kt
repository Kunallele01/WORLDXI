package com.dreamxi.app.feature.finalize

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import com.dreamxi.app.core.ui.PlaceholderScreen

/** Full XI on the pitch, last position swaps, then Simulate Season. */
@Composable
fun FinalizeScreen(modifier: Modifier = Modifier) {
    PlaceholderScreen(title = "Finalize", modifier = modifier)
}
