package com.dreamxi.app.feature.history

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import com.dreamxi.app.core.ui.PlaceholderScreen

/** Past draft runs and results, synced from Supabase with local (Room) caching. */
@Composable
fun HistoryScreen(modifier: Modifier = Modifier) {
    PlaceholderScreen(title = "History", modifier = modifier)
}
